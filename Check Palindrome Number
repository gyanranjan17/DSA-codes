#include <iostream>
#include <math.h>
using namespace std;
bool isPalindrome(int n)
{
    int temp=n,rev=0;
    while(temp!=0)
    {
        rev=rev*10 + temp%10;
        temp=temp/10;
    }
    return(rev==n);
}
int main()
{
   int x;
   cin>>x;
   cout<<isPalindrome(x);
   return 0;
}
